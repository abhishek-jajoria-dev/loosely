# loosely
Paste any string and turn it into valid JSON with Loosely. Format messy data, minify JSON, filter and inspect content, validate syntax, export results, and keep a history of everything you've parsed. Quickly identify JSON errors, clean up malformed data, and transform raw text into structured, usable JSON.
